# Copy to .env and fill in. .env is gitignored; .env.example is committed.

# Base58 private key exported from your Solana wallet (Phantom: Settings →
# Security & Privacy → Show Private Key). ~87-88 characters. Used by
# scripts/load-wallet.ts to generate .anchor/deploy-wallet.json.
DEPLOY_PRIVATE_KEY=
